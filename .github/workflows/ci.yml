name: CI

on:
  push:
    branches: [ "**" ]

permissions:
  contents: read
  id-token: write    # required for Workload Identity Federation

jobs:
  build-test-terraform:
    runs-on: ubuntu-latest

    steps:
      # ------------------------
      # Checkout code
      # ------------------------
      - name: Checkout
        uses: actions/checkout@v4

      # ------------------------
      # Node.js section
      # ------------------------
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Lint (optional)
        run: npm run lint --if-present

      - name: Build
        run: npm run build --if-present

      # ------------------------
      # Authenticate to GCP using Workload Identity Federation (WIF)
      # ------------------------
      - name: Authenticate to GCP (WIF)
        id: auth
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ secrets.GCP_WIF_PROVIDER }}
          service_account: ${{ secrets.GCP_SA_EMAIL }}
          token_format: 'access_token'
          access_token_lifetime: '3600s'

      # ------------------------
      # Setup gcloud (uses credentials from the auth step)
      # ------------------------
      - name: Setup gcloud
        uses: google-github-actions/setup-gcloud@v2

      # Set project without triggering token refresh
      - name: Configure gcloud project
        run: |
          gcloud config set project curamet-onboarding --installation

      # Verify authentication
      - name: Verify GCP Authentication
        run: gcloud auth list --filter=status:ACTIVE --format="value(account)"

      # Set access token for Terraform
      - name: Set access token for Terraform
        run: |
          echo "GOOGLE_OAUTH_ACCESS_TOKEN=${{ steps.auth.outputs.access_token }}" >> $GITHUB_ENV

      # ------------------------
      # Terraform section
      # ------------------------
      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.7.5

      - name: Terraform Init & Plan
        working-directory: terraform
        env:
          GOOGLE_OAUTH_ACCESS_TOKEN: ${{ steps.auth.outputs.access_token }}
        run: |
          terraform init -backend-config="environments/dev/backend.conf"
          terraform plan -var-file="environments/dev/terraform.tfvars" -input=false -no-color
